// PrimaryRouter is the router that will be used throughout the project.
// It has to use the same router so that new routes can be created 
// without restarting the server

var express = require("express");
var PrimaryRouter = express.Router();
module.exports = PrimaryRouter;
